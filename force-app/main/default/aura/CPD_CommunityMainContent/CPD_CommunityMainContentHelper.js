({
    clickGetStarted: function (component, event) {
        document.querySelector('.registry-form-section').scrollIntoView({ 
            block: 'end', 
            behavior: 'smooth', 
            inline: 'center'
          });
    }
})