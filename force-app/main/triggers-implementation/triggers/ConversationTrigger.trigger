/**
 * Created by Igor Malyuta on 24.12.2019.
 */

trigger ConversationTrigger on Conversation__c (before insert, before update, before delete, after insert, after update, after delete, after undelete) {

}